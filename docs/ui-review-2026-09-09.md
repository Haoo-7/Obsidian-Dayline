# Dayline 前端审查与重构建议

日期：2026-09-09。范围：桌面月历、时间线、心情编辑及设置入口。

## 实施与验收更新

### 键盘焦点转移回归：真机确认修复（2026-09-13）

用户真机确认：先聚焦“添加自定义感受”，再点“心情备注”时无法切换焦点的问题已解决。修复保留现有的键盘可视区域算法，只收紧触摸期间的几何同步时机。

- 根因：iOS 从上方输入框切到下方备注时会产生 visualViewport 平移；`scroll` 与 `focusout` 在同一触摸内重算弹窗几何并滚动内容，弹窗在手指下移动，浏览器随后取消了目标字段的 focus/click。
- 修复位于 `src/mood-modal-viewport.ts`：`touchstart` 时取消待执行帧并冻结几何同步；`touchend`/`touchcancel` 后只同步一次；`focusout` 触发的 reveal 同样延后，由新的 activeElement 决定滚动目标。没有改动键盘高度算法或表单 DOM。
- 自动检查：47 个测试文件、401 项测试通过；`npm run typecheck`、`npm run build`、`git diff --check` 通过。触控焦点切换回归测试位于 `tests/mood-modal-viewport.test.ts`。
- 部署：构建包与 Mac Sandbox 部署包逐字节一致，SHA-256 为 `16fa9ef681e92e4f9c96f21a4cdc57f73b10d583e41b2877366f6997dc27ba42`。Syncthing `sandbox` 完成度为 100%，iPhone 对该 `main.js` 的 availability 为 `fromTemporary: false`。`data.json` 与 `Calendar/journal-metadata.json` 均与部署前基线逐字节一致。
- 清理：Sandbox 中仅用于定位的 `Calendar/dayline-keyboard-geometry.json` 已移除；证据副本保留在 `local-evidence/2026/09/11/01a09075-2db1-7431-862d-ddd319b67559/keyboard-stable/mac-only-geometry.json`。

### 天气 chip 质感回退与横屏紧凑布局（2026-09-13，待真机验收）

用户指出手机天气信息丢失了原有毛玻璃质感，并确认横屏心情页可视区域过小。本轮没有继续扩大手机视觉重构范围，只做两项定向修正。

- 天气 chip：保留浮动毛玻璃外观，但移动端挂载后读取宿主 `.view-header` 的实际高度，把 chip 定位到 header 下方 8px，避免再和工具栏按钮挤在同一行。桌面端保持原有浮动 chip。对应回归在 `tests/weather-overlay-lifecycle.test.ts`。
- 横屏心情页：在 `orientation: landscape` 下启用 `is-compact-viewport` 专用规则，减少顶部与安全区占用，隐藏步骤说明和步骤二摘要，将流体图形、读数、轨道和备注框压缩；返回/保存不再常驻占位，随内容滚动。竖屏、桌面、草稿和保存契约不变。
- 自动检查：47 个测试文件、400 项测试通过；`npm run typecheck`、`npm run build`、`git diff --check` 通过。部署包 SHA-256 为 `ac0d78c2b7a3ad3611f95644428bab94943f214e029263577b619d4e8172f754`；Syncthing `sandbox` 完成度 100%，iPhone availability 为 `fromTemporary: false`。
- Sandbox 重载只改变了天气缓存，已把 QA 后文件留证为 `data-after-qa.json`，并把 `data.json` 恢复为部署前基线；`Calendar/journal-metadata.json` 始终逐字节一致。真机结论等待用户复验天气行位置和横屏第一、二步的滑动与保存可达性。

### 移除未保存关闭确认（2026-09-13，待真机验收）

用户指出手机心情弹窗几乎占满屏幕，误触外部的概率很低；而确认层会锁住其他按钮，不符合移动端操作路径。该确认已整体移除。

- 关闭、点弹窗外部或按 Escape 现在都会直接关闭并丢弃当前弹窗草稿，不再显示“还有未保存的心情更改”。
- 保存某一日期后直接关闭，即使其他日期仍有未保存草稿；日期切换期间按文件保留会话草稿的逻辑不变。
- 删除内容包括弹窗确认分支、确认层 DOM/CSS、`keepEditing` 与 `unsavedMoodChanges` 文案及对应回归测试。最新自动检查为 47 个测试文件、399 项测试通过；`typecheck`、`build`、`diff check` 通过。
- 本版部署包 SHA-256 为 `02c6e35279fe49705408b91776387ec5c4ef105bdf5534807c775837afc1057d`。写入时 Syncthing 尚未完成向 iPhone 的传输，手机 availability 待复验；Sandbox 天气缓存变化已留证并恢复测试前数据。

### 下一阶段：完成手机 P1 收尾（待真机验收）

键盘焦点回归已关闭，但本批手机 P1 还不能整体结案。按剩余风险，下一步只需逐项复验当前实现，不再新增范围：

1. 心情滑块：在第一步纵向触摸滚动时，心情值不被误调；横向明确拖动后才更新。
2. 手机天气 chip：真实天气下保持毛玻璃质感，位于宿主 header 下方，不与工具栏按钮重叠。
3. 横屏键盘：备注聚焦后弹窗不塌缩，输入内容可见，能够滚动到保存按钮；开关键盘至少三轮。

这三项通过后，再进入最后一个收尾批次：设置表单、空状态和中英文一致性检查。Android、平板仍不在本批范围。

### 键盘动画后塌缩：历史诊断（2026-09-11 22:37 用户反馈）

以下记录当时待修状态，已被 2026-09-13 的真机确认修复取代。

用户截图 `用户的 2026-09-11 失败截图` 确认：自定义感受或备注聚焦后短暂可用，随即弹窗压成顶部窄条，键盘仍显示。前述宿主交集补丁未解决完整键盘动画流程；377 项测试和无键盘真机检查不构成该缺陷已修复的证据。

- 只读核验桌面 Obsidian 1.13.7 包，证实 `.app-container` 受 `--keyboard-height` 限制，但未找到 iOS 原生键盘回调或 `--safe-area-inset-bottom` 包含键盘高度的赋值。因此重复扣减与宿主反馈目前均是待验证假设，不是已确认根因。
- **当前构建包含临时诊断代码，不是可发布修复包。** `src/mood-modal-viewport.ts` 仅在库名为 `Obsidian Sandbox` 时记录最近 80 帧的视口、宿主、弹窗尺寸及相关 CSS 数值，写入 `Calendar/dayline-keyboard-geometry.json`，不记录输入文本或日记内容。定位完成后须移除该诊断代码、重新构建部署并清理诊断文件。
- 诊断包类型检查、构建及 diff-check 通过，已部署 Mac Sandbox。Mac 手机类名探针确认日志可写出并包含预期几何字段；探针结束后恢复类名并关闭未修改表单。Mac 产生的记录由 `bodyClass` 中的 `mod-macos` 区分，不能当作手机数据。
- 等待手机操作：Synctrain 同步 → 停用/启用 Dayline → 点备注复现一次 → 再打开 Synctrain 回传。22:49 的诊断包为 1,209,677 字节，当时 Syncthing availability 为空，未确认手机已收到或加载。
- 数据基线及证据目录：`local-evidence/2026/09/11/01a09075-2db1-7431-862d-ddd319b67559/keyboard-collapse/`。用户的原始失败截图应保留。

### 原生键盘高度修复（2026-09-11 晚，第二次接续）

用户再次真机确认键盘仍然遮挡表单，前一轮仅依赖 VisualViewport 的修复不能视为解决该问题。本次由 medium 子代理实施窄范围修复，主代理检查真实宿主 CSS、复核代码并完成 Sandbox 运行时验证。

- 根因证据：本机 Obsidian 1.13.7 `app.css` 定义 `body.is-mobile .app-container { max-height: calc(100vh - var(--keyboard-height)); }`，键盘动画期间另有覆盖。原实现只读取 `visualViewport`/`innerHeight`，遗漏了原生键盘仅缩短宿主区域、但视口尺寸不变的情况。
- 修复位于 `src/mood-modal-viewport.ts`：取可视视口与实际 `.app-container`、`.modal-container` 边界的交集；用 ResizeObserver 及根节点/宿主 style、class 观察器跟随键盘变化；不再次扣减键盘高度。保留输入节点、焦点及手动滚动，关闭时清理观察器和待执行帧。
- 增加 7 项回归，覆盖宿主 overlay 模式、resize 模式不重复扣减、带偏移的 modal 宿主、样式与键盘动画状态变化、保存按钮可达及关闭清理。全量 47 个文件、377 项测试通过；类型检查、构建及 diff-check 通过。
- 真实宿主 CSS 验证（Mac Sandbox 手机布局探针，不是真机）：将 `--keyboard-height` 设为 520px，`visualViewport.height` 前后均为 923px；宿主底边变为 403px，弹窗底边随之变为 391px、高度 379px。备注原节点保留且可见；滚动到底后保存按钮范围 314.22–358.22px，位于内容可视范围 97–390px 内。此验证没有直接伪造宿主的测量结果或触发窗口 resize。
- 探针初次在隐藏窗口中等待动画帧，未立即更新；将 Sandbox 窗口置前后实际观察器运行并得到上述结果。探针结束后关闭未修改表单、恢复 body 类名及样式并移除 QA 临时状态。Sandbox 未捕获运行时错误。
- 部署：只更新 Mac Sandbox 的 `main.js`，21:24 构建包为 1,208,114 字节；Syncthing 文件级 availability 已确认 iPhone 持有该版本，非临时文件。文件已收到不等于运行中插件已重载。
- 真机状态：镜像恢复，但当前手机表单有用户刚输入的未保存测试草稿，因此未擅自舍弃或重载。已询问是否可舍弃后重载；新键盘补丁的真机结论仍待重载与实际软键盘复验，横屏、天气占位行等未验收项保持待办。
- 本次证据及数据基线目录：`local-evidence/2026/09/11/01a09075-2db1-7431-862d-ddd319b67559/keyboard-followup/`。
- 收尾仅发现天气缓存变化，已保存变化证据、恢复本次基线并重新加载内存设置；两个数据文件与基线均逐字节一致。构建包与 Mac Sandbox 部署包也逐字节一致。手机未保存的测试草稿未被操作。

#### 手机重载复验（2026-09-11 22:20）

- 用户明确允许舍弃测试草稿，解锁镜像后已点击“舍弃更改”，没有保存该草稿。
- 手机 Obsidian Sandbox 中已停用并重新启用 Dayline。停用时设置页退出且 Dayline 叶子关闭，随后重新进入第三方插件设置完成启用；未修改主笔记库。
- 新版心情弹窗顶部已避开状态栏，点击关闭按钮可正常返回时间线；重新打开后两步流程可用，备注聚焦成功。截图为 `keyboard-followup/phone-reloaded-safe-area.png` 和 `keyboard-followup/phone-reloaded-note-focus.png`。
- 镜像中的备注聚焦仍未唤起软键盘。已把未输入内容的表单留在第二步，请用户直接在手机上触发软键盘，确认输入内容可见且可以滚动到保存按钮，不需要实际保存。因此原生键盘遮挡的最终真机结论仍未确认，不能用这两张无键盘截图替代。
- 此次没有改动源码，无需重复已通过的 377 项测试。Mac Sandbox 的配置逐字段与本次基线一致，心情元数据逐字节一致。

### 遮挡与滚动接续验收（2026-09-11 晚）

接续用户下午五张截图对应的修复。前一任务已经实现 VisualViewport 适配、纵向触摸与心情横向拖动的区分、工具栏间距和手机天气占位行；本次复核实现、完成自动检查，并继续在 iPhone 13 mini 的 Obsidian Sandbox 验收。整体手机验收尚未完成。

- 当前实现：心情弹窗随键盘可视区域调整尺寸，保留表单节点与草稿；纵向触摸不立即改变心情，横向意图明确后才拖动；底部操作随内容滚动。手机模式切换栏与下方工具栏之间增加间距。天气在手机笔记标题和内容之间独立占位，桌面浮层位置保持不变。
- 真机已验证：手机测试库名称为 Obsidian Sandbox；Synctrain 恢复连接后，Syncthing 文件级 availability 确认旧阶段最终包已在 iPhone，随后停用/启用 Dayline。深色月历、时间线两排工具栏已分开；心情两步、主动打开日期选择器、备注聚焦和 Escape 退出可用，未输入或保存测试心情。
- 新发现与补丁：可视区域适配后的心情弹窗进入状态栏区域，顶部关闭按钮连续点击无响应，Escape 可退出。`src/plugin.ts` 改为整个弹窗四边避开安全区域，并扣除相应可用宽高，不再只给内部内容增加安全区 padding。此补丁已构建、部署 Mac Sandbox；20:56 的 Syncthing 文件级 availability 确认 iPhone 已收到 1,206,770 字节的新包，`fromTemporary: false`。这只证明文件传输，不证明手机已加载最后补丁。
- 最后补丁的手机重载/复验被镜像服务中断：Computer Use 连续返回 ScreenCaptureKit `-3812`、`-3811` 画面捕获错误。因此关闭按钮的最后补丁未完成真机验收；此前截图只能证明补丁前的工具栏和心情表单，不能用于证明最终安全区修复。
- 最终自动检查：47 个测试文件、370 项测试通过；`npm run typecheck`、`npm run build`、`git diff --check` 通过。检测器对相关实现返回 `[]`，不作为视觉验收依据。
- Mac Sandbox 最终运行包回归：桌面心情内容宽度 `clientWidth === scrollWidth === 586`，没有启用手机可视区域绑定；进入第二步后焦点为 `H3`，原生关闭按钮可退出，未捕获运行时错误。现版宿主关闭按钮类名为 `modal-header-button`，不能只用旧的 `modal-close-button` 选择器判断关闭行为。
- 安全区域布局探针（桌面 DOM，不是真机）：375×812、上下安全边距 50/34 时，弹窗为 `(10, 50, 355, 728)`；812×170、左右安全边距 44 时，弹窗为 `(44, 12, 724, 146)`。两种情况均在给定安全边界内；后者内容可视高度 89、滚动高度 453。探针结束后恢复 body 类名并关闭弹窗。
- 数据收尾：与前一任务留下的基线比较，`data.json` 只有 `weatherCache` 变化，已保留变化证据并恢复原文件、重新加载内存设置；`data.json` 与 `Calendar/journal-metadata.json` 最终均和 `/tmp/dayline-followup-W3afCi/` 中基线逐字节一致。最终 `main.js` 与 Mac Sandbox 部署包逐字节一致。手机恢复文件的后续同步未另行确认。
- 未验收：真实软键盘弹出后的备注/保存可达性、横屏纵向手势不误调心情、有天气数据时独立占位行与宿主按钮互不遮挡、最后安全区域补丁的手机关闭操作。镜像中备注可以聚焦但没有弹出软键盘；本轮未操作物理旋转。Android、平板仍不在本批范围。
- 证据目录：`local-evidence/2026/09/11/01a09075-2db1-7431-862d-ddd319b67559/dayline-acceptance/`。包含深色月历/时间线工具栏、心情两步截图及缓存变化证据。仅部署 Sandbox，未部署主笔记库、提交或发布。

### iPhone 接续验收（2026-09-11）

本次通过 iPhone Mirroring 操作 iPhone 13 mini 的 Obsidian Sandbox，完成可通过镜像验证的主流程，并修复真机发现的三处样式问题。不是完整移动端验收：软键盘遮挡、旋转及完整屏幕阅读器流程仍未验证；Android、平板不在本批范围。

- 来源表单：新版表单已加载，新增来源聚焦目录；舍弃更改后来源恢复为空，没有应用测试配置。
- 时间线：年月分组、筛选展开、日期选择、收藏筛选及清除可用。收藏标签点击后结果由 24 条变成 7 条，清除恢复 24 条；起始日期设为 2026-09-01 后显示 2 条。日期空值与选值均未越过两列边界。
- 心情：浅深色两步均检查。初次打开不自动唤起日期选择，主动点击后原生选择器可用；标签选择、关闭时未保存提示、舍弃后返回可用。没有保存测试心情。
- 导航：Markdown 日历快捷入口、月历/时间线切换、月份面板及返回原笔记通过；受控往返过程标签数保持 3。调试时停用插件会关闭 Dayline 叶子，标签数降为 2，重新打开恢复 3；该维护行为不计入普通返回流程。
- 真机修复仅涉及 `src/plugin.ts` 的 CSS：44px 触控尺寸不再直接放大收藏复选框，保留标签触控高度；日期输入取消原生 appearance 的尺寸干预；心情 `.modal-content.journal-mood-picker` 同节点设置 16px 内容边距，避免原来的 `padding: 0` 覆盖。
- 验证过程中手机同步曾断开。14:40:47 Syncthing 文件级 availability 确认最终 `main.js` 已在 iPhone，非临时数据；Sandbox 完成度 100%，待同步项和字节均为 0。随后手机停用/启用 Dayline，15:04–15:05 真机最终复验确认心情日期右侧留白、两步内容边距和日期选择器均正常。
- 自动检查：本批初次补丁全量 342 项通过，后续定向 38 项及最终定向 14 项通过；类型检查、最终构建和 diff-check 通过。最终构建与 Mac Sandbox 逐字节一致；仅更新测试库，未部署主笔记库。
- 测试主题已恢复为“跟随系统”。来源及心情草稿已舍弃。天气自动刷新改变了 `data.json` 缓存，已保留修改后证据并恢复测试前文件；`data.json` 与 `Calendar/journal-metadata.json` 最终均和测试前备份逐字节一致。Mac Sandbox 未捕获错误，不以此代表手机控制台无错误。
- 证据目录：`local-evidence/2026/09/09/01a08658-ccc0-77a2-b334-be8907095ecd/mobile-20260911/`。最终截图为 `mood-accepted-light.png`、`mood-step2-accepted-light.png`；筛选修复为 `timeline-fixed.png`。深色截图在最终日期 appearance 补丁之前，不能当作该最后补丁的深色复验。

更新于 2026-09-09。下文原始审查保留为改动前的记录，其中“没有修改源码”和旧行号只描述审查当时，不代表当前交付状态。

本轮完成时间线与心情编辑的重点修复，保留照片月历、流体心情图形及现有存储契约，没有迁移框架或整体重写。

| 范围 | 已实施 |
|---|---|
| 时间线层级 | 按年月分组；文件时间明确标为创建或更新；统计默认折叠并标明全部日记范围 |
| 时间线布局 | 使用面板容器查询；窄栏缩略图 68px、常规 88px；摘要 13px；弱化条目边框，补齐焦点轮廓 |
| 筛选 | 展开不重建工具栏；补齐可见字段名、条件数量、逐项移除及空结果恢复入口 |
| 标题编辑 | 索引刷新保留输入节点；筛选排除正在编辑的条目时单独保留草稿；失败就地重试或取消，避免异步完成抢走焦点 |
| 心情流程 | 初始值明确显示“平稳”，采用原建议中允许的默认值方案；日期可用 Tab 到达；第二步聚焦标题 |
| 心情草稿 | 按文件缓存会话草稿；添加标签不重建备注框；保存使用快照并锁定相关操作；失败保留输入，关闭时直接丢弃当前弹窗草稿 |
| 心情视觉 | 外围使用主题背景与标准按钮；标签选中增加勾选；移除整表反复播放的模糊入场动画 |

关键实现位于 `src/journal-timeline-view.ts`、`src/mood-picker-modal.ts`，配套样式与中英文文案位于 `src/plugin.ts`、`src/i18n.ts`。草稿缓存仅在当前弹窗会话内有效，不是跨重启持久保存；保存当前日期会直接关闭，日期切换期间的会话草稿仍会保留。

### 验证证据

- `npm test`：44 个测试文件、306 项测试通过，覆盖标题草稿、筛选焦点、心情快照、保存锁定、失败重试及已有移动端回归。测试中的故障日志来自预设失败场景。
- `npm run typecheck`、`npm run build`、`git diff --check` 通过；界面检测器返回 `[]`，不以此替代视觉判断。
- 最终构建发现上一段的 `main.js` 尚未包含最后一批标题草稿修复；已重新生成并仅更新 Obsidian Sandbox。部署文件与工作区最终构建逐字节一致，未部署到 Main_Topic。
- 最终 Sandbox 实测：编辑标题后切换到无结果筛选，草稿进入保留区域，输入节点、焦点和选区 `[4, 9]` 不变；恢复筛选后条目回到列表；Escape 取消成功，保存调用为 0。
- 上一段已验证 260/320/400px 时间线容器无横向溢出，以及心情步骤、日期往返和自定义标签的无写入流程；本次复看了对应浅深色心情截图与时间线截图。最后补丁的受影响路径另按上一条重新实测。
- Sandbox 未捕获运行时错误。测试草稿已取消，主题恢复浅色；重载自动补入的空 `tombstones` 字段在验证结束后按备份恢复，配置未变。

### 仍待处理

上述三项已在下面的接续验收中完成。手机仅有现有自动回归，本轮没有完成真机、手机键盘遮挡或完整屏幕阅读器验收。

### 接续验收（2026-09-09）

- 接手时工作区已包含来源编辑器、本地化及月历焦点的未验收实现；本次复核代码、运行测试，并补上来源类型下拉框的 Obsidian `dropdown` 样式。
- 来源设置改为目录、名称、类型、日期字段及启用状态表单，保留高级 JSON。编辑只更新草稿，点击应用才保存；校验错误就地显示；保存失败保留草稿，索引刷新失败可以重试而不重复保存。
- 创建日记确认框、日历条目数量和天气辅助标签已接入中英文文案。月历导航保留当前控件焦点；Escape 收起月份选择并回到标题；异步完成时不抢回已移到外部的焦点。
- `npm test`：46 个文件、342 项测试通过。`npm run typecheck`、构建及 `git diff --check` 通过；最后的下拉框样式补丁另行重新构建并在真实窗口复看。
- Obsidian Sandbox 实测：新增来源聚焦目录，编辑未改变插件设置，测试草稿已舍弃；来源表单在 320、400、600px 容器下 `scrollWidth === clientWidth`；真实设置窗口截图已检查。该窄栏检查不代表手机真机验收。
- 月历实测 Escape 后焦点回到标题，下一月加载后焦点仍在下一月按钮，测试后恢复原月份。
- 最终构建仅部署至 Sandbox，未部署 Main_Topic。配置文件 SHA-1 未变；元数据唯一变化为重载自动补入空 `tombstones`，移除该字段后的序列化内容 SHA-1 与测试前一致，未修改已有心情记录。

## 结论与证据边界

Dayline 有明确的产品特色：以真实照片和日期回看生活，心情是补充信息。粗糙感主要来自信息优先级和交互收尾不一致，而不是缺少装饰。不建议整套换肤，建议保留照片月历、主题变量和流体心情控件，重构其外围布局与状态。

本次为降级单上下文审查：两个独立审查 agent 均因服务 502 失败，由主 agent 完成源码和运行时核对。没有修改插件源码或日记。

- 运行环境：Main_Topic，Obsidian 1.13.7，Dayline 2.2.0，当前第三方浅色主题。运行包与未提交源码未做逐字节一致性验证，以下明确区分实测与源码判断。
- 通过 Obsidian CLI 和 Computer Use 检查运行时 DOM、计算样式、焦点和可访问性树。窗口截图始终返回旧画面，且 `document.visibilityState` 为 `hidden`，因此不以这些截图证明当前视觉效果。
- 仓库 `screenshots/dayline-showcase-filters.png` 仅用来理解历史布局；其中日期和当前源码已经不同，不当作当前缺陷证据。
- `detect.mjs --json src` 返回 `[]`。扫描器未报问题不等于界面合格，尤其本项目使用动态 DOM 和内嵌 CSS。
- 未注入浏览器覆盖层；未验证手机、深色主题或屏幕阅读器完整流程。外部规范页面检索未返回可用内容，不宣称完成 WCAG 合规检查。下文尺寸和动效时间是建议的项目规格。

## 优先问题

### P1：时间线丢失月份上下文

实测完整日期节点为 `display: none`。源码只展示星期和日号，列表没有月份分组；跨月浏览时“周三 9”无法单独确定日期。时间还取自 `modifiedAt || createdAt`，没有说明是修改时间，容易被理解为记录发生时间。

位置：`src/plugin.ts:2005`，`src/journal-timeline-view.ts:32`、`:435`、`:486`。

修复：按 `YYYY-MM` 分组并显示“2026 年 9 月”；每条保留完整日期的可访问名称。时间不可靠时不展示；展示文件时间时明确标注“更新于”，不要冒充事件时间。

### P1：筛选展开导致键盘焦点丢失

实测将焦点放在筛选按钮后触发点击，展开状态变为 `true`，但 `document.activeElement` 变成 `BODY`。原因是点击调用 `render()`，后者 `root.empty()` 删除了当前控件。月份跳转也使用相同整区重建方式，存在类似风险，尚未单独实测。

位置：`src/journal-timeline-view.ts:184`、`:273`；`src/plugin.ts:2777`。

修复：工具栏与筛选表单只创建一次，展开只改 `hidden` 和 `aria-expanded`；结果更新限定在列表、数量和已选条件。关闭浮层返回触发按钮；删除条件后聚焦邻近条件或筛选按钮。

### P1：心情未选择状态和默认值互相矛盾

实测第一步显示“选择一个强度”，但“继续”可用；直接继续后变为“平稳”，焦点直接落到“保存”。这把没有表达的感受隐式转成了一个有效值。日期输入框实测 `tabIndex === -1`，鼠标可用但正常 Tab 不可达。

位置：`src/mood-picker-modal.ts:90`、`:128`、`:245`。

修复：推荐未选择时禁用继续，第一次明确点击、拖动或键盘调整后启用。另一种可接受方案是从初始就明确显示“平稳”并使控件值为 0，但不能同时显示“请选择”。日期恢复 `tabIndex=0`，首次聚焦控制单独处理。第二步聚焦标题或首个感受选项，不默认聚焦提交。

### P1：编辑草稿没有完整的失败与离开路径

源码中标题保存失败会 `render()`，新输入内容没有恢复；索引推送也可能整区重建打断编辑。心情切换日期会覆盖当前 `score/labels/note`；保存时只禁用保存按钮，返回和日期仍可操作。自定义标签添加后重新渲染并再次聚焦保存按钮。以上是源码风险，未通过真实写入或故障注入验证。

位置：`src/journal-timeline-view.ts:531`；`src/mood-picker-modal.ts:144`、`:216`、`:249`。

修复：草稿独立于 DOM，编辑期间保留；失败显示就地错误和重试，不清空输入。日期切换按目标路径保存会话草稿。保存请求使用提交时快照并锁定会改变目标的操作；已有未保存内容时离开采用保留草稿或明确舍弃流程。

### P2：侧栏空间分配没有围绕阅读任务设计

当前运行时有图条目宽约 320px，正文列仅约 152px。源码在侧栏内仍固定使用 104px 缩略图，并以整个窗口 `@media (max-width: 420px)` 判断紧凑模式。大窗口里的窄侧栏不会触发该规则。

标题缺失时反复显示“添加标题”，把阅读列表变成待填写表单；日期 24px、摘要 12px、时间 10px，在有限空间内优先放大了日号而非正文。始终显示的连续记录统计进一步推迟列表起点。

位置：`src/plugin.ts:1992`、`:2004`、`:2017`、`:2020`、`:2108`；`src/journal-timeline-view.ts:453`。

修复：按面板容器宽度适配。默认摘要 13px、行高 1.5、日期 18–20px、次要信息 12px；窄侧栏缩略图 64–72px。没有标题时直接从摘要开始，编辑入口放到条目菜单和焦点可达操作中。统计默认一行摘要，详细趋势按需展开。

### P2：筛选的表单层级和统计范围不明确

日期字段有 `aria-label` 和 `title`，但没有可见的“开始/结束”标签；各类字段平铺排列，清除用孤立的 X。结果数量随筛选变化，统计仍取全部条目，用户可能认为它们属于同一范围。无结果只显示一句文字，无就地恢复操作。

位置：`src/journal-timeline-view.ts:218`、`:280`、`:335`、`:409`。

修复：日期范围独立成组，其他筛选使用带字段名的紧凑布局；激活条件显示数量及可单独移除的标签；明确“全部日记统计”，或让统计跟随筛选并标明范围。空结果提供清除筛选，空资料库提供创建日记/配置来源。

### P2：视觉语言和交互反馈不统一

源码中的心情弹窗使用径向渐变、整面随心情着色、彩色阴影、胶囊按钮；月历使用紧凑平面按钮。感受选中态主要靠淡色背景和阴影，识别不够直接。每次第二步重建都会重新触发 360ms 模糊入场动画。时间线焦点只改变右边框颜色，同时移除 outline。

位置：`src/plugin.ts:1999`、`:2024`、`:2050`、`:2080`。

修复：保留流体图形作为心情表达主体，外围恢复主题背景和标准按钮。选中标签增加勾选标志。焦点使用完整 2px 轮廓。按钮反馈建议 100–140ms，面板切换 160–200ms；不对每次更新播放整表动画。已有 reduced-motion 支持应保留。

### P2：普通设置暴露实现细节，本地化也有断口

Journal sources 要求手写 JSON，并在每次输入时解析和保存；编辑过程中暂时无效的 JSON 会弹 Notice。创建日记弹窗硬编码英文，中文环境仍显示英文。月历可访问名称里的 `entries`、`weather available` 同样硬编码。

位置：`src/settings-tab.ts:236`；`src/plugin.ts:2865`、`:4305`。

修复：来源使用文件夹选择、名称和日期字段组成的可编辑行，JSON 只放高级入口，显式应用后验证；错误显示在字段旁。所有用户可见与辅助技术文案进入 `i18n.ts`。

## 建议保留

- 月历中的真实图片是产品识别点，不需要新插画或装饰背景。
- 大多数界面已经使用 Obsidian 主题变量，图标沿用宿主 `setIcon`。
- 搜索输入当前使用局部 `updateResults()`，不是每次输入都重建工具栏；应推广这个模式。
- 流体控件已有键盘处理、状态播报及 reduced-motion 支持，不能笼统判定为“不支持无障碍”。
- 缩略图已有懒加载和过期请求保护，应在布局重构中保留。

## 重构方案

采用“保留产品特色，重整呈现与状态”的路线。仅换 CSS 无法解决焦点和草稿问题；迁移 React 或整套重写又没有必要。

1. 先修日期语义、筛选焦点、心情显式选择、编辑失败保留草稿。
2. 建立很小的样式基础：4/8/12/16px 间距、6px 控件圆角、最多 8px 条目圆角、主题背景/边框/文字变量。心情色仅用于图形、标记和选中反馈。
3. 时间线工具栏固定节点，结果列表按月分组；以面板容器查询控制缩略图和内容密度。
4. 心情编辑保持两步流程，补齐状态、日期草稿缓存、就地错误和焦点恢复。不要同时改存储契约。
5. 最后整理设置表单、空状态和中英文一致性。

## 关键实现示例

以下是重构骨架，未接入生产代码、未执行测试，不应直接视为完成补丁。`mount/update` 等名称是建议新增的内部方法，不是当前已有 API。

### 稳定筛选节点

```ts
// renderFilters 首次创建时保存引用；展开不再调用整区 render()。
const setFilterOpen = (open: boolean) => {
  this.filterMenuOpen = open;
  menu.hidden = !open;
  filterButton.setAttribute('aria-expanded', String(open));
  if (!open && menu.contains(document.activeElement)) filterButton.focus();
};
filterButton.addEventListener('click', () => {
  setFilterOpen(!this.filterMenuOpen);
});
// 删除原 is-hidden 分支；确保 hidden 不被 display:flex 覆盖。
```

```css
.journal-timeline-filter-menu[hidden] { display: none; }
.journal-timeline-view { container-type: inline-size; }
.journal-timeline-entry:focus-visible,
.cal-icon-button:focus-visible {
  outline: 2px solid var(--interactive-accent);
  outline-offset: -2px;
}
@container (max-width: 360px) {
  .journal-timeline-entry.has-thumbnail {
    grid-template-columns: 28px minmax(0, 1fr) 68px;
    gap: 8px;
    padding: 8px;
  }
  .journal-timeline-thumbnail,
  .journal-timeline-thumbnail img {
    width: 68px;
    min-width: 0;
    height: 68px;
  }
}
```

使用容器查询时，容器必须是条目的祖先。现有手机和粗指针样式的优先级需一起核对；容器规则不替代触摸目标尺寸规则。

### 心情草稿与提交状态

```ts
type MoodDraft = {
  filePath: string;
  score: number | null;
  labels: string[];
  note: string;
};
type MoodEditorState =
  | { status: 'editing'; step: 1 | 2; draft: MoodDraft }
  | { status: 'saving'; step: 2; draft: MoodDraft }
  | { status: 'error'; step: 2; draft: MoodDraft; message: string };
```

状态规则：

| 操作 | 状态变化 | 用户反馈 |
|---|---|---|
| 未选择心情 | `editing`，score 为 null | 继续禁用，不暗中默认值 |
| 明确选择 | score 为 -2 到 2 | 图形、文字及继续同步更新 |
| 继续 | step 1 → 2 | 聚焦步骤标题或首个感受 |
| 添加自定义标签 | 只更新标签区域 | 保持输入焦点，不重播整页动画 |
| 切换日期 | 按 filePath 缓存/恢复 draft | 切换前后草稿可找回 |
| 保存 | 快照 → saving | 显示“保存中”，锁定重复提交和目标切换 |
| 保存失败 | saving → error | 保留草稿、就地错误、重试 |
| 保存成功 | 关闭并刷新对应条目 | 焦点返回发起编辑的条目/按钮 |
| Escape | 根据 dirty 与 saving 状态处理 | 不把尚未提交的草稿无声清空 |

不需要引入状态机库。用上述联合类型或等价的小型控制器集中控制按钮、离开和提交分支即可。保存回调必须拿快照，不能在异步完成后再读已经切换的日期。

## 验收

- 月份切换、过滤条件删除、后台索引更新后，焦点及正在输入的草稿不丢失。
- 时间线跨年跨月可定位；没有标题的日记仍然易读，不反复显示待填写提示。
- 心情流程可仅用键盘完成，日期可达；未选择不等于平稳；失败可重试且不丢草稿。
- 在 260/320/400px 侧栏、主面板、浅深色主题和长中文标签下检查裁切与布局。
- 只为行为风险补测试：焦点保留、草稿保留、保存锁定、失败重试。视觉值用截图检查，不为每条 CSS 写复述测试。
- 本轮没有运行生产测试，因为没有修改生产代码。实际运行时验证只覆盖上述无写入路径。

## 启发式粗评

这是有限证据下的设计判断，不是可访问性认证。

| 项目 | 分数 / 4 | 主要原因 |
|---|---:|---|
| 状态可见 | 2 | 有加载态；保存主要靠禁用和透明度 |
| 符合真实语义 | 2 | 日期缺少月份，文件修改时间含义不明 |
| 用户控制 | 1 | 草稿离开和切换日期策略不足 |
| 一致性 | 2 | 主题变量较好，弹窗和普通控件语言不同 |
| 错误预防 | 2 | 有提交禁用，仍有隐式默认值 |
| 识别优于记忆 | 2 | 图标有名称，日期和筛选字段可见语义不足 |
| 操作效率 | 2 | 搜索和键盘入口存在，整区重绘打断流程 |
| 简洁性 | 2 | 图片特色明确，辅助信息占位较多 |
| 错误恢复 | 1 | 标题失败丢草稿，空结果缺少恢复入口 |
| 帮助与说明 | 2 | 有文档及 tooltip，普通配置仍暴露 JSON |
| 合计 | 18 / 40 | 优先补齐交互，再做视觉统一 |
